# Task: 补全缺失的 CSS 样式 + 集成 Toast 到关键场景 + Bug 修复

## 现状
以下 TypeScript 文件已创建完毕，但对应的 CSS 样式完全缺失，功能无法正常显示：
- `apps/web/src/stores/toastStore.ts` — toast store 完成
- `apps/web/src/components/Toast.tsx` — Toast + ShortcutHintBar 组件完成
- `apps/web/src/hooks/useKeyboard.ts` — 键盘快捷键完成
- `apps/web/src/components/Skeleton.tsx` — 骨架屏组件完成
- `apps/web/src/App.tsx` — 已集成 Toast/MiniPlayer/Keyboard/ShortcutHintBar

## 需要完成的工作

### 1. 在 apps/web/src/styles/global.css 末尾添加以下所有缺失的 CSS 样式

需要添加的样式类别（用已有的 CSS 变量如 --mint, --bg-glass, --border, --text-primary, --text-secondary, --text-muted, --radius, --radius-sm, --t-fast 等）：

#### Toast 通知
- `.toast-container` — fixed 定位，右上角，z-index 9999，flex column，gap 8px
- `.toast` — glassmorphism 风格，padding 12px 16px，border-radius，cursor pointer，slide-in-right 动画
- `.toast-success` — 左边框绿色
- `.toast-error` — 左边框红色
- `.toast-info` — 左边框蓝色
- `.toast-icon` — 图标样式
- `.toast-message` — 文字样式
- `@keyframes slideInRight` — 从右侧滑入

#### 快捷键提示
- `.shortcut-hint` — fixed 定位，底部居中（bottom 100px），z-index 9998，glassmorphism 背景，fade 动画

#### MiniPlayer 底部栏
- `.mini-player` — fixed bottom 0，left 0，right 0，z-index 900，glassmorphism，backdrop-blur，border-top
- `.mini-progress` — 3px 高的进度条
- `.mini-progress-fill` — mint 色填充
- `.mini-content` — flex，align-items center，padding 8px 16px
- `.mini-cover` — 40x40px，border-radius 6px
- `.mini-cover-fallback` — 默认音符图标
- `.mini-info` — flex 1
- `.mini-title` — 13px，白色
- `.mini-artist` — 11px，灰色
- `.mini-controls` — 右侧按钮
- `.mini-btn` — 32x32px，圆形播放按钮
- `.has-mini-player` — main-content 加 padding-bottom 70px 给 MiniPlayer 让位

#### 骨架屏
- `.skeleton` — 带 shimmer 脉冲动画的占位块
- `@keyframes skeleton-shimmer` — 左右移动的高光效果
- `.skeleton-card` — 透明度稍低的卡片

#### 歌曲推荐卡片（确认是否已存在，只添加缺失的）
检查以下样式是否已存在，只添加缺失的：
- `.song-cards`, `.song-card`, `.song-card-cover`, `.song-card-play-overlay`, `.song-card-info`, `.song-card-title`, `.song-card-artist`, `.song-card-reason`, `.song-card.streaming`

#### 聊天布局修复
确认 `.chat-bubble.user` 的 flex-direction 是 row-reverse（用户消息头像在右），如果不是则修复

### 2. 集成 Toast 到关键场景

在以下文件中添加 toast 提示（import { useToastStore } from "../stores/toastStore"）：

#### apps/web/src/pages/PlayerPage.tsx
- 保存歌单成功后：`addToast("歌单已保存", "success")`
- 保存歌单失败后：`addToast("保存失败", "error")`

#### apps/web/src/stores/playerStore.ts
- 播放错误时：`addToast("播放失败，已跳过", "error")` (在 onError 回调中)
- 注意：import toastStore 时避免循环依赖，直接 import useToastStore

#### apps/web/src/stores/chatStore.ts
- 收到 onError 时：`addToast("AI 对话出错", "error")`

### 3. Bug 检查
- 检查 `apps/web/src/pages/PlayerPage.tsx` 中是否有未使用的 import（如 useState, useCallback 如果不再需要的话）
- 确保 ChatArea 不接收 props（它从 store 自己读取）
- 确保 IntentInput 不接收 props

## 约束
- 暗色主题，glassmorphism 风格
- 使用已有的 CSS 变量
- TypeScript strict, 4-space indent
- 只追加 CSS 到 global.css 末尾，不要修改已有的样式规则
