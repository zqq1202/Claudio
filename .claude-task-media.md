Task: Add MediaSession API to AudioPlayer.ts

Current file: apps/web/src/audio/AudioPlayer.ts
It wraps HTMLAudioElement. Need to add browser MediaSession integration.

Requirements:
1. Add a `setMediaCallbacks(cb)` method that accepts callbacks: { onNext, onPrevious }
2. Add an `updateMetadata(title: string, artist: string, coverUrl: string)` method
3. In `updateMetadata`, set `navigator.mediaSession.metadata = new MediaMetadata({ title, artist, album: 'Claudio', artwork: [{ src: coverUrl }] })`
4. In constructor or after load, register MediaSession action handlers:
   - play → this.audio.play()
   - pause → this.audio.pause()
   - previoustrack → callbacks.onPrevious()
   - nexttrack → callbacks.onNext()
   - seekto → this.seek(details.seekTime * 1000) with fastSeek if available
5. Export the singleton `audioPlayer` as before

Then in apps/web/src/stores/playerStore.ts, after audioPlayer.onPlay callback setup, add:
```typescript
audioPlayer.setMediaCallbacks({
  onNext: () => get().next(),
  onPrevious: () => get().previous(),
});
```
And in `playItem`, call `audioPlayer.updateMetadata(item.title ?? '', item.artist ?? '', item.coverUrl ?? '')` after loading.

Also fix bug: in playerStore.ts onError callback, check that `audio.duration` is not NaN before accessing it.

Constraints: TypeScript strict, 4-space indent. Only modify the two files mentioned.
