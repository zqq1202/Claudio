Phase 1: Implement core music player features. Do ALL of the following in order:

1.1 Volume Control:
- Add `volume`, `isMuted`, `setVolume()`, `toggleMute()` to playerStore.ts
- Add volume slider + mute toggle to PlayerControls.tsx in the `.player-bar-right` area
- Add CSS styles for volume-slider in global.css

1.2 Shuffle / Repeat modes:
- Add `repeatMode` (off/all/one), `shuffle` boolean, `shuffledQueue` to playerStore.ts
- Implement `toggleShuffle()`, `cycleRepeat()`, modify `next()` and `previous()` logic
- Add shuffle/repeat buttons to PlayerPage.tsx controls-row (NOT PlayerControls)
- Add CSS styles with mint highlight for active state

1.3 Song Search:
- Create apps/server/src/routes/search.ts with GET /api/search?q=xxx&limit=10 using ncm.search()
- Register it in apps/server/src/index.ts
- Add searchSongs() to api client.ts
- Create apps/web/src/components/SearchPanel.tsx with search input + results list
- Wire search into PlayerPage (button in top-nav or as toggleable panel)
- Add CSS for search-panel

1.4 Play error toast:
- Add `lastError` state to playerStore
- Show inline error banner when playback fails

IMPORTANT: 
- Use 4-space indent, TypeScript strict
- Follow existing code style (look at other components for patterns)
- DO NOT modify files you dont need to change
- Add all new CSS to apps/web/src/styles/global.css at the end of the file
- Make sure imports are correct
