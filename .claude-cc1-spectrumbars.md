# Task: Rewrite SpectrumBars — Make It Come Alive

## Context
SpectrumBars is the bottom-of-page audio frequency bar visualization.
Currently it's flat, not responsive to music, and looks dead.

## Changes Required

### 1. SpectrumBars.tsx — Full Rewrite
File: `/projects/Claudio/apps/web/src/components/SpectrumBars.tsx`

Replace the ENTIRE file with a rewrite. Keep the same interface (`{ active }` prop), same import pattern, same canvas approach. Make these specific changes:

#### A. FFT Setup — More Resolution
```typescript
analyser.fftSize = 1024;  // was 256 (128 bins) → now 512 bins
analyser.smoothingTimeConstant = 0.7; // was 0.8, less built-in smoothing = more reactive
```

#### B. Logarithmic Frequency Mapping
Replace the current linear mapping:
```typescript
// OLD: const idx = Math.floor(Math.pow(i / BAR_COUNT, 1.6) * freqData.length);
// NEW: logarithmic mapping for perceptually correct frequency distribution
const minLog = Math.log10(1);
const maxLog = Math.log10(freqData.length);
const logIdx = Math.pow(10, minLog + (maxLog - minLog) * (i / BAR_COUNT));
const idx = Math.min(Math.floor(logIdx), freqData.length - 1);
```

#### C. Perceptual Bass Boost
Boost lower frequencies since human hearing is less sensitive there:
```typescript
const ratio = i / BAR_COUNT;
const boost = 1.0 + Math.pow(1.0 - ratio, 2.0) * 1.5;
target = Math.min(1, (freqData[idx] || 0) / 255 * boost);
```

#### D. Better Smoothing (Attack/Release)
```typescript
// OLD: bars[i] += (target - bars[i]) * (target > bars[i] ? 0.35 : 0.12);
// NEW: Fast attack (snap up), slow release (float down)
bars[i] += (target - bars[i]) * (target > bars[i] ? 0.5 : 0.88);
```

#### E. Height Scaling — Sqrt Compression
```typescript
// OLD: const barHeight = bars[i] * h * 1.3;
// NEW: sqrt compression for better dynamic range
const barHeight = Math.pow(bars[i], 0.7) * h * 0.85;
```

#### F. Peak Indicators
Add peak tracking refs:
```typescript
const peaksRef = useRef<number[]>(new Array(BAR_COUNT).fill(0));
const peakHoldRef = useRef<number[]>(new Array(BAR_COUNT).fill(0));
```

In draw loop, after computing barHeight:
```typescript
// Peak indicator
if (bars[i] > peaksRef.current[i]) {
  peaksRef.current[i] = bars[i];
  peakHoldRef.current[i] = 30;
} else if (peakHoldRef.current[i] > 0) {
  peakHoldRef.current[i]--;
} else {
  peaksRef.current[i] *= 0.97;
}
const peakY = h - Math.pow(peaksRef.current[i], 0.7) * h * 0.85;
ctx.fillStyle = toRgba(primaryColor, 0.7);
ctx.fillRect(x, peakY - 2, barWidth - gap, 2);
```

#### G. Glow Effect
```typescript
ctx.shadowColor = primaryColor;
ctx.shadowBlur = 6 + bars[i] * 10;
// Reset shadow after drawing: ctx.shadowBlur = 0;
```

#### H. Better Gradient — Use Primary Color
```typescript
const grad = ctx.createLinearGradient(x, h, x, y);
grad.addColorStop(0, toRgba(primaryColor, 0.02));
grad.addColorStop(0.3, toRgba(primaryColor, 0.1));
grad.addColorStop(0.7, toRgba(primaryColor, 0.35));
grad.addColorStop(1, toRgba(primaryColor, 0.7));
```

#### I. Rounded Top Caps
```typescript
const radius = Math.min(3, (barWidth - gap) / 2);
ctx.beginPath();
ctx.roundRect(x, y, barWidth - gap, barHeight, [radius, radius, 0, 0]);
ctx.fill();
```

#### J. Idle Animation (when not active)
Keep the existing idle sine wave but make it more visible:
```typescript
target = 0.15 + 0.12 * Math.sin(t * 0.003 + i * 0.4) * Math.sin(t * 0.001);
```

#### K. Add `toRgba` helper
The file needs its own color helper since it uses canvas gradients. Copy from AudioVisualizer:
```typescript
function toRgba(color: string, alpha: number): string {
  const m = color.match(/^#([0-9a-f]{6})$/i);
  if (m) {
    const r = parseInt(m[1], 16) >> 16;
    const g = (parseInt(m[1], 16) >> 8) & 0xff;
    const b = parseInt(m[1], 16) & 0xff;
    return `rgba(${r},${g},${b},${alpha})`;
  }
  if (color.startsWith("rgb(")) {
    return color.replace("rgb(", "rgba(").replace(")", `,${alpha})`);
  }
  return `rgba(94,232,197,${alpha})`;
}
```

### 2. PlayerPage.tsx — Default Mode
File: `/projects/Claudio/apps/web/src/pages/PlayerPage.tsx`
Line 45: Change `useState<ModeKey>("Glob")` → `useState<ModeKey>("Arcs")`

### 3. global.css — Spectrum Bars Height
File: `/projects/Claudio/apps/web/src/styles/global.css`
Find the rule at line ~4056 with `.spectrum-bars-container` and change `height: 80px;` → `height: 120px;`

## IMPORTANT
- Keep the same component interface (Props: `{ active: boolean }`)
- Keep the canvas-based approach (no new dependencies)
- Keep `usePlayerStore` subscribe pattern for isPlaying if needed
- Do NOT modify AudioVisualizer.tsx
- Do NOT create new files
- Read ALL existing code in each file before modifying
